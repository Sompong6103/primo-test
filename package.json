{
    "name": "primo-test",
    "version": "1.0.0",
    "private": true,
    "type": "commonjs",
    "main": "dist/merge.js",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "jest",
        "test:watch": "jest --watchAll",
        "start": "node dist/src/merge.js",
        "dev": "nodemon --watch src --ext ts --exec ts-node src/merge.ts"
    },
    "devDependencies": {
        "@types/jest": "^29.5.14",
        "jest": "^29.7.0",
        "nodemon": "^3.1.11",
        "ts-jest": "^29.2.5",
        "ts-node": "^10.9.2",
        "typescript": "^5.9.3"
    }
}
